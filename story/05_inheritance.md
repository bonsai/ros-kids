# 第5話　部品を交換した

## あらすじ

シロの吠え声は「ワン」だけ。
もっと色々なバリエーションが欲しい。

タクミは「スーパーロボット犬」へのアップグレードを決意する。
ベースはそのままに、新しい機能だけを追加したい——**継承**の登場。

---

## 本文

「ワン！」

シロが吠えた。元気いっぱいだ。

でも、それだけだ。いつも「ワン」。

「もう少しバリエーションが欲しいな…」
「日に何度も同じ『ワン』だと、さすがに…」

タクミは考える。

「シロの基本機能はそのままに、『声のバリエーション』だけ追加したい」
「でも、シロのクラスを直接書き換えるのは——なんか違う気がする」

サービスマニュアルの、さらに奥のページに、こんな記載があった。

```
RS-01 は拡張モジュールに対応しています。
専用ドーターボードを接続することで、
音声出力のバリエーションを増やすことができます。
```

「ドーターボード……拡張基板か！」

タクミはネットで中古の拡張基板を探した。
幸い、互換品が安く出ていた。

基板をシロの背中にある拡張スロットに差し込む。
カチッ、と音がした。

「これでハードウェアはOK。次はソフトウェアだ…」

---

## 継承

Java には **継承** という仕組みがある。
既存のクラスをベースに、新しいクラスを作る。

```java
// ベースのロボット犬
class RobotDog {
    private String name;
    private int battery;

    RobotDog(String name) {
        this.name = name;
        this.battery = 100;
    }

    void bark() {
        System.out.println(name + "：ワン！");
    }
}

// 拡張版：継承で作る
class SuperRobotDog extends RobotDog {

    SuperRobotDog(String name) {
        super(name);  // ベースの初期化を呼ぶ
    }

    // 新しい機能を追加
    void barkLoudly() {
        System.out.println("ガウォーン！！");
    }

    void barkSoftly() {
        System.out.println("……ぅ");
    }
}
```

```java
public class Main {
    public static void main(String[] args) {
        SuperRobotDog shiro = new SuperRobotDog("シロ");
        shiro.bark();        // ベースの機能（ワン！）
        shiro.barkLoudly();  // 新しい機能（ガウォーン！！）
        shiro.barkSoftly();  // 新しい機能（……ぅ）
    }
}
```

「ベースの機能はそのままに、新しい機能が追加できた！」

タクミはコンパイルして転送した。

「シロ、試しに」

「ワン！」

「うん。次、大声で」

「ガウォーン！！」

タクミは隣の部屋を気にした。

「……今のはデカすぎた。小さい声で」

「……ぅ」

「完璧だ……！」

---

## オーバーライド（上書き）

「でも待てよ。bark() 自体を変えたいときはどうするんだ？」

Java では、親クラスのメソッドを **オーバーライド（上書き）** できる。

```java
class SuperRobotDog extends RobotDog {

    @Override
    void bark() {
        System.out.println(getName() + "：ワンワンワン！（元気いっぱい）");
    }
}
```

`@Override` って書くと、「親のメソッドを上書きしてるよ」という意味になる。

---

タクミは机に突っ伏したシロの頭を撫でた。

「すごいぞシロ。お前、『継承』と『拡張』ができるようになったんだ」

シロは——撫でられていることには気づいてないようだった。
バッテリー残量が少なかったからだ。

## 知ったこと

- **継承（extends）**：既存のクラスをベースに新クラスを作る
- **super**：親クラスを指す。コンストラクタは `super()` で呼ぶ
- **オーバーライド**：親のメソッドを子クラスで上書きする
- **@Override**：コンパイラに「上書きしてるよ」と伝える印

## 次回予告

スーパーロボット犬になったシロ。
でも、タクミの友達が「うちのペットロボットも動かしたい」と言い出した。
別のメーカーの、別の設計のロボットだ。

「全部『ロボット犬』として同じ操作で動かせないかな…」
